<h2>New book</h2>

<%= render 'error', book: book %>

<%= form_with model: book,local:true do |f| %>
  <p>Title</p>
  <%= f.text_field :title %>

  <p>Body</p>
  <%= f.text_area :body %>

  <div><%= f.submit "Create Book" %></div>
<% end %>